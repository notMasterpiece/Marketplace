import React from 'react';
import Header from '../../components/Header/HeaderContainer';

function Policy() {
  return (
    <div>
      <Header isSearch={false} />
      <div className="container">Policy</div>
    </div>
  );
}

export default Policy;
